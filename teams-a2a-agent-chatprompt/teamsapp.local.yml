# yaml-language-server: $schema=https://aka.ms/teams-toolkit/1.0.0/yaml.schema.json
#
# The teamsapp.local.yml composes automation tasks for M365 Agents Toolkit when running locally.
version: 1.0.0

environmentFolderPath: ./env

provision:
  - uses: teamsApp/create
    with:
        name: TeamsA2aChatPrompt${{APP_NAME_SUFFIX}}
    writeToEnvironmentFile:
        teamsAppId: TEAMS_APP_ID

  - uses: botAadApp/create
    with:
        name: TeamsA2aChatPrompt${{APP_NAME_SUFFIX}}
    writeToEnvironmentFile:
        botId: BOT_ID
        botPassword: SECRET_BOT_PASSWORD

  - uses: botFramework/create
    with:
        botId: ${{BOT_ID}}
        name: TeamsA2aChatPrompt
        messagingEndpoint: ${{BOT_ENDPOINT}}/api/messages
        description: 'Teams bot using ChatPrompt with A2AClientPlugin'
        channels:
            - name: msteams

  - uses: teamsApp/validateManifest
    with:
        manifestPath: ./appPackage/manifest.json

  - uses: teamsApp/zipAppPackage
    with:
        manifestPath: ./appPackage/manifest.json
        outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
        outputJsonPath: ./appPackage/build/manifest.${{TEAMSFX_ENV}}.json

  - uses: teamsApp/update
    with:
        appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip

deploy:
  - uses: file/createOrUpdateEnvironmentFile
    with:
        target: ./.env
        envs:
          PORT: 3978
          CLIENT_ID: ${{BOT_ID}}
          CLIENT_SECRET: ${{SECRET_BOT_PASSWORD}}
